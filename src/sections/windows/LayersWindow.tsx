import React from "react";
import styled from "styled-components";

const StyledLayersWindow = styled.div`
  width: 100%;
  height: 100%;
`;

const LayersWindow = () => {
  return <StyledLayersWindow></StyledLayersWindow>;
};

export default LayersWindow;
