import React from "react";
import styled from "styled-components";

const StyledCanvas = styled.div`
  height: 40rem;
  width: 60rem;
  background-color: white;
`;

const Canvas = () => {
  return <StyledCanvas></StyledCanvas>;
};

export default Canvas;
