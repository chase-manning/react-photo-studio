import React from "react";
import styled from "styled-components";

const StyledColorPicker = styled.div`
  width: 100%;
  height: 100%;
`;

const ColorPicker = () => {
  return <StyledColorPicker>meow</StyledColorPicker>;
};

export default ColorPicker;
